= form_for @user_phone do |f|
  - if @user_phone.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user_phone.errors.count, "error")} prohibited this user_phone from being saved:"
      %ul
        - @user_phone.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
